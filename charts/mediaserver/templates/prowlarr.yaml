apiVersion: v1
kind: Service
metadata:
  name: {{ include "mediaserver.fullname" . }}-prowlarr
  labels:
    io.kompose.service: prowlarr
  {{- include "mediaserver.labels" . | nindent 4 }}
  annotations:
    kompose.version: 1.26.1 (a9d05d509)
spec:
  type: {{ .Values.prowlarr.type }}
  selector:
    io.kompose.service: prowlarr
  {{- include "mediaserver.selectorLabels" . | nindent 4 }}
  ports:
	{{- .Values.prowlarr.ports | toYaml | nindent 2 -}}