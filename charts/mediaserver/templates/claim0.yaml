apiVersion: 
kind: PersistentVolumeClaim
metadata:
  name: {{ include "mediaserver.fullname" . }}-claim0
  labels:
    io.kompose.service: sonarr-claim0
  {{- include "mediaserver.labels" . | nindent 4 }}
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
status: {}